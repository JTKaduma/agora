# Issue 226: Loyalty Points Best-Effort Error Handling

### DESCRIPTION

> **Note:** Continuous Integration (CI) must pass for any submission. Contributors are required to write and pass unit tests for their specific changes.

> Ensure that if `update_loyalty_score` fails, the payment still finishes successfully.

### TASKS

- [ ] Wrap loyalty update in a match that ignores Err.
- [ ] Add test case simulating registry failure during payment.
- [ ] **Unit Tests**: Implement corresponding unit tests in `test.rs` to verify your changes.
- [ ] **CI Compliance**: Ensure all GitHub CI checks pass (run `cargo test` and `cargo clippy` locally).

**Requirement:** Make sure GitHub CI check passes before submitting.

**Depends on:**
None
